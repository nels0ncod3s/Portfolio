<!--
	Loading curtain. Deliberately CSS-only (see `.intro` in app.css) so it always
	clears — even if hydration is slow or JavaScript never arrives.
-->
<div class="intro" aria-hidden="true">
	<span class="intro__num"></span>
	<span class="intro__bar"></span>
</div>
